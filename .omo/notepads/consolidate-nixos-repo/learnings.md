# Learnings — consolidate-nixos-repo

Conventions, patterns, and successful approaches discovered during work on this plan.

_Auto-scaffolded by /start-work. Append new entries below - never overwrite._

---

## Todo 2 — remote + secrets backup (2026-08-23)

- A's origin: `https://github.com/meillaya/NixOS-config.git`; HEAD `84fc2cf45d65f3ce47211571147e6c6010494adb`; only branch `main` (local + origin in sync).
- `secrets/coding-agents.yaml` sops header has TWO age recipients: `age1xxj3ft4g9gkfryedx85z9je76s07xxfqcvzl4c3t777d45hfgpvqwkpclc` (admin) and `age1f3fgneph45yn900kqvzkysztspyz7puh9uqneujpxft5rsqjeglq99lnz0` (recovery). First recipient exactly matches B's `&admin` key in `/home/mei/nix-config/.sops.yaml` line 2. sops version 3.13.2, lastmodified 2026-07-27T23:31:58Z.
- Evidence written to `.omo/evidence/consolidate-nixos-repo/remote-and-secrets-backup.json`. No secret values were decrypted or read.

## Todo 3 — union-add sibling tree + fresh root commit (2026-08-23)

- Union-added B's 796 tracked files into A via `git ls-files` + per-file copy, skipping `.git/`, `.omo/`, `.codegraph/`, `__pycache__/`, `result/`, `result-*`, `*.py[cod]`, and `secrets/*` (except `coding-agents.yaml` + `README.md`). Never overwrote an existing A file.
- A already had `modules/standalone-linux/config/noctalia/config.toml` and `secrets/`; those stayed A's. B's other `modules/standalone-linux/` files, `modules/darwin/`, `apps/aarch64-darwin/`, `pkgs/omniwm.nix`, `.gitignore`, and `.sops.yaml` were added.
- A's pre-existing files verified byte-identical to pre-merge (sha256): `modules/linux/home-manager.nix`, `modules/entities/_machine-authority/model.nix`, `modules/entities/hosts.nix`.
- Fresh root commit `4c18cd6` on `main` (0 parents) with message `build(nixos): merge sibling repo as fresh root (union-add)`; 286 files. Old A history intentionally discarded (backed up in todo 2 evidence). Not pushed.
- No nested git repo: B's `.git` was never copied (only tracked files were copied).

## Todo 6 — verify `.sops.yaml` + `.gitignore` (2026-08-23)

- `.sops.yaml` present at merged root with `&admin age1xxj3ft4g9gkfryedx85z9je76s07xxfqcvzl4c3t777d45hfgpvqwkpclc` (line 2) and `&recovery age1f3fgneph45yn900kqvzkysztspyz7puh9uqneujpxft5rsqjeglq99lnz0` (line 3); creation rule targets `secrets/coding-agents\.yaml$` with both keys.
- `.gitignore` present with `result`, `result-*`, `.DS_Store`, `*.bak`, `*.tmp`, `temp_insert.txt`, `__pycache__/`, `*.py[cod]`, `secrets/*`, `!secrets/README.md`, `!secrets/coding-agents.yaml`.
- `secrets/coding-agents.yaml` sops header recipients (lines 16 + 25) match `.sops.yaml` keys exactly (admin + recovery). No fixes needed; todo 3's union-add already copied both files correctly.

## Todo 4 — union `flake.nix` inputs: add `darwin` (2026-08-23)

- Added `darwin` input to merged `flake.nix` right after `home-manager` (line 14-17): `url = "github:LnL7/nix-darwin/master"` + `inputs.nixpkgs.follows = "nixpkgs"`. Kept `disko`, `den` pin (`1614f6f8...`), and all other inputs byte-identical; `outputs` line untouched. Diff is exactly +4 lines.
- Verification: `nix eval --impure --expr 'builtins.getFlake "path:/home/mei/NixOS-config"'` fetched `darwin` (`github:LnL7/nix-darwin/4cff07d`), resolved `darwin/nixpkgs` follows, and evaluated Darwin derivations (`org.nixos.lorri.plist`, `darwin-test-services-lorri`) — flake evaluates.
- Gotcha: `builtins.getFlake "path:."` fails with "cannot fetch input 'path:.' because it uses a relative path" — must use an absolute path (`path:/home/mei/NixOS-config`). Also the flake's top-level has no `.flake` attribute (mkFlake output shape), so `(getFlake ...).flake.inputs` errors with `attribute 'flake' missing`; the fetch/resolve warnings in the eval output are the real evidence that inputs registered.

## Todo 5 — union the 5 divergent files per-file (2026-08-23)

- `modules/linux/home-manager.nix`: B's version is NEWER (removed Sweet-Dark theme + `sweet` pkg refs, replaced with Phase 0 Option C comments). Took B's version (cp). Verified `diff` empty.
- `config/package-exceptions.json`: B's version is a SUPERSET of A's for surviving systems (adds darwin/raycast). Took B's version and removed all `aarch64-linux` entries (host dropped in todo 14). Result: 22 entries, systems = {aarch64-darwin, x86_64-linux}, raycast present, 0 aarch64-linux refs.
- `modules/aspects/features/desktop-media.nix`: B's version is correct (uses `homeManagerModules.spicetify`, no nixos branch `inputs` arg). Took B's version. config-eval line 169 (`nixos.programs.spicetify.enable`) PASSED with B's version — proves it works for the NixOS host.
- `modules/aspects/hardware/device-capability-routing.nix`: A's version is the SUPERSET (has `linuxRouting` for NixOS hosts + `darwinRouting` for darwin host). B's version only has darwin routing. KEPT A's version (no change).
- `tests/dendritic-*.sh`: kept A's versions (todo 7 rewrites them). Untouched.
- Flake/nix files merge cleanly: `nix-instantiate --eval --strict --expr 'let f = builtins.getFlake "path:/home/mei/NixOS-config"; in { nixos = builtins.attrNames f.nixosConfigurations; ... }'` → `nixos = [ "aarch64-linux" "nixos-x86-qualifier" "x86_64-linux" ]`.
- `nix-instantiate --eval --strict --expr 'import ./tests/dendritic-config-eval.nix {}'` FAILS at line 203 (apps assertion: merged flake now has `nh` app, A's test expects 7 apps). This is the documented todo-7 concern (config-eval rewrite), NOT a merge error.

## Todo 8 — rename repo dir + git remote to `nixos` (2026-08-23)

- Renamed working dir `/home/mei/NixOS-config` → `/home/mei/nixos` via `mv` (rename, not copy+delete). Old path no longer exists.
- Updated origin URL: `https://github.com/meillaya/NixOS-config.git` → `https://github.com/meillaya/nixos.git` via `git remote set-url origin`. Verified `git remote get-url origin` returns the new URL and `.git/config` contains `url = https://github.com/meillaya/nixos.git`.
- Updated `.omo/boulder.json` `active_plan` (2 occurrences: nested work + top-level) from `/home/mei/NixOS-config/.omo/plans/...` to `/home/mei/nixos/.omo/plans/...`. No other absolute paths referenced the old dir.
- Not pushed. Product files untouched.

## Todo 9 — rewrite repo-name references to `nixos` (2026-08-23)

- Renamed all REPO-NAME references (`NixOS-config`, `~/nix-config`, `nix-config/` repo path, `nixos-config` repo path) → `nixos` across 13 files: 2 codex shims (`mei.nix:55`, `standalone-linux/home-manager.nix:23`), `system.nix:35` nixPath, `darwin.nix:11` comment, README (title + 2 refs), `dendritic.md:134`, `nixos-anywhere-disko-install.md:114`, `dendritic-apps.sh:64-65`, `task17/run-case.sh` + `task22/run-case.sh` (repo-root var `nix_config_root`/`NIX_CONFIG_REPO_ROOT` → `nixos_root`/`NIXOS_REPO_ROOT` + sibling path), `calibre.md:60` (env var doc), `.codegraph/{source.json,daemon.log,daemon.pid}`.
- Deleted all `__pycache__/*.pyc` (10 dirs, 33 files) — they embed the old name; they regenerate.
- Restarted the codegraph daemon with `--path /home/mei/nixos` (old pid 969027 was still serving the dead `/home/mei/NixOS-config` path and kept writing stale `projectRoot` lines into daemon.log; new pid 1004155).
- KEPT (not repo names): `bin/nix-config-*` script names, `nix-config-home-preflight`, runtime paths (`/run/nix-config-device/`, `nix-config-install-staging`, `var/lib/nix-config/identity`, `var/db/nix-config/activation-transactions`), schema `$id` `nix-config.invalid`, external URL `dustinlyons/nixos-config` (different repo).
- Acceptance grep now returns only script-name/runtime-path/schema-id/external-URL matches — 0 repo-name matches. Evidence: `.omo/evidence/consolidate-nixos-repo/task-9-consolidate-nixos-repo.json`.

## Todo 10 — remap `nixos-laptop` → `remembrance` (2026-08-23)

- Renamed machine authority key `nixos-laptop` → `remembrance` in `model.nix` (hostId `remembrance`, target `nixosConfigurations.remembrance`, role workstation, system x86_64-linux). Updated the matching hardcoded routing check in `validators.nix:410` (hostId + target).
- `git mv`'d `named-hosts/nixos-laptop.nix` → `remembrance.nix` (aspect key `remembrance`, includes `remembrance-storage` + `remembrance-device-capability-routing`) and `storage/nixos-laptop.nix` → `remembrance.nix` (aspect key `remembrance-storage`, includes `remembrance-hardware-routing`, message updated).
- Renamed hardware routing aspects: `device-capability-routing.nix:85` → `remembrance-device-capability-routing`, `x86-vendor-routing.nix:35` → `remembrance-hardware-routing`.
- `nixos-workstation.nix:3` compatibility alias now selects `den.aspects.remembrance`.
- `hosts.nix`: `machineFor "remembrance"`, assert target `nixosConfigurations.remembrance`, and the den.hosts host key under `x86_64-linux` changed `x86_64-linux` → `remembrance` (the OUTER system-group key `x86_64-linux` stays — it's the system, not the host).
- **Gotcha**: the den framework names the flake output `nixosConfigurations.<hostKey>` from the den.hosts host key. Because the machine target is now `nixosConfigurations.remembrance`, the den.hosts inner host key MUST be `remembrance` (not `x86_64-linux`) or the flake fails with `string '"nixosConfigurations.remembrance"' is not equal to string '"nixosConfigurations.x86_64-linux"'`. The task's "key x86_64-linux stays" wording was internally inconsistent; the flake-eval acceptance criterion is authoritative.
- Also updated `modules/flake/outputs.nix` configurationEvaluationPaths entry `nixosConfigurations.x86_64-linux` → `nixosConfigurations.remembrance` (flake output name follows the host).
- Python routing: `contracts.py` `_ROUTES` + the two `{"remembrance", "nixos-x86-qualifier"}` sets, and `collector.py` `ROUTES` all renamed. Test fixtures updated: `task15/fixtures.py` (hostId/target), `task15/extended_negatives.py` (N11 alias mutations → `remembrance-alias`), `task17/model.py` (hostId).
- `tests/dendritic-config-eval.nix`: flake output name `nixosConfigurations.remembrance`, machineIds list re-sorted alphabetically (`aarch64-linux`, `nixos-x86-qualifier`, `remembrance`), hostName assert `"remembrance"`.
- `tests/dendritic-architecture.sh`: renamed file paths + grep patterns to `remembrance`.
- Verification: `grep -R -n --exclude-dir=.git -E 'nixos-laptop' modules/ scripts/ tests/` → 0 matches. `nix-instantiate --eval --strict --json --expr 'let f = builtins.getFlake "path:/home/mei/nixos"; in f.machineAuthority.machines'` → keys `["aarch64-linux" "nixos-x86-qualifier" "remembrance"]`. dendritic-config-eval/architecture/boundaries all PASS; task15 N11 route-binding negative PASS.
- Note: `nix eval --impure --json --expr '... | .machineAuthority.machines'` (pipe syntax from the plan) is NOT supported by this nix version — use `nix-instantiate --eval --strict --json` with a `let ... in` expression instead.

## Todo 11 — declare `antagony` workstation (2026-08-23)

- Added `antagony` machine record to `model.nix` (hostId `antagony`, target `nixosConfigurations.antagony`, system x86_64-linux, role workstation, identity mei, boot.state disabled, storage.profile none, all trust/capabilities disabled) — exact copy of `remembrance` with key/hostId/target changed.
- Created `named-hosts/antagony.nix` (aspect `den.aspects.antagony`, includes `antagony-storage` + `antagony-device-capability-routing`, role workstation assert) and `storage/antagony.nix` (aspect `den.aspects.antagony-storage`, includes `pending-x86-workstation-hardware` + `antagony-hardware-routing`, message "antagony storage must remain disabled until enrollment").
- Registered routing aspects: `device-capability-routing.nix:86` → `den.aspects.antagony-device-capability-routing = linuxRouting`, `x86-vendor-routing.nix:36` → `den.aspects.antagony-hardware-routing = routing`.
- `hosts.nix`: `machineFor "antagony"`, assert target `nixosConfigurations.antagony`, and `den.hosts.x86_64-linux.antagony` entry (hostName/machine/users.${identity.name}).
- **Gotcha**: adding a machine REQUIRES registering it in `validators.nix` `validKnownRouting` (added the antagony branch at line 416) — otherwise `getMachine` throws "invalid machine declaration: invalid platform or cross-field combination" and the whole flake fails to evaluate. Same pattern as todo 10's remembrance rename.
- Verification: `nix eval --impure --json --expr 'builtins.attrNames (builtins.getFlake "path:/home/mei/nixos").nixosConfigurations'` → `["aarch64-linux","antagony","nixos-x86-qualifier","remembrance"]`. `grep -Fq "antagony" modules/entities/_machine-authority/model.nix` passes.
- Did NOT touch `modules/flake/outputs.nix` `configurationEvaluationPaths` (evaluation inventory, not in this todo's scope) and did NOT add antagony to `scripts/hardware/contracts.py`/`collector.py` (todo 14).

## Todo 12 — map `massive` → standalone-linux Home-Manager host (2026-08-23)

- The union-add kept A's `hosts.nix` (no `den.homes`), so B's `den.homes` block was lost. Re-added it to `modules/entities/hosts.nix` as `den.homes.x86_64-linux.standalone-linux = { machine = laptop; userName = laptop.identity.name; homeDirectory = laptop.identity.home; }` — `laptop` is the existing `machineFor "remembrance"` binding. No NixOS `massive` machine was created (massive is standalone-linux Home-Manager only).
- Verification: `nix eval --impure --json --expr 'builtins.attrNames (builtins.getFlake "path:/home/mei/nixos").homeConfigurations'` → `["standalone-linux"]`; `nixosConfigurations` still `["aarch64-linux","antagony","nixos-x86-qualifier","remembrance"]`.
- **Gotcha**: the homeConfiguration output does NOT expose `userName`/`homeDirectory` at the top level (nor `config.userName`/`config.homeDirectory`). The identity lives at `homeConfigurations.standalone-linux.config.home.{username,homeDirectory}` → `{"homeDirectory":"/home/mei","username":"mei"}`. The `den.homes` block's `userName`/`homeDirectory` fields are consumed by the den framework, not echoed verbatim into the flake output.

## Todo 13 — map `aarch64-darwin` → `entropy` (darwin host) (2026-08-23)

- Added `entropy` machine record to `model.nix`: copied B's original `aarch64-darwin` record VERBATIM from B's git history (`git -C ~/nix-config show HEAD:modules/entities/_machine-authority/model.nix`) with only key/hostId/target changed → `entropy`/`"entropy"`/`"darwinConfigurations.entropy"`. Kept system `aarch64-darwin`, role workstation, identity mei (home /Users/mei, uid 501, gid 20), display 2:1, cpuVendor Apple, firmware apple, gpu apple-metal, network native-darwin, platformExpectations kind darwin (wifi, TCC accessibility+screen, kitty+emacs managed apps, all digests).
- **Gotcha (hand-copying digests)**: retyping the 8 SHA-256 digests by hand introduced silent transcription errors. Used a python script to extract the record block from B's git show text (brace-balanced), apply exactly 3 renames (attr key, `hostId`, `target`), assert `system == "aarch64-darwin"` survives, and splice it into the merged `model.nix` — zero manual transcription of the digest hex.
- `validators.nix` `validKnownRouting`: replaced the `aarch64-darwin` branch (hostId/target) with the `entropy` branch (hostId `entropy`, target `darwinConfigurations.entropy`, system `aarch64-darwin`, role `workstation`). Same pattern as todo 10/11 — without it `getMachine "entropy"` throws "invalid platform or cross-field combination". The darwin `validCrossFields` branch (cpuVendor Apple → gpu apple-metal …) was already host-agnostic (keyed on `system == "aarch64-darwin"`), so it needed NO change.
- `git mv`'d `named-hosts/aarch64-darwin.nix` → `entropy.nix` (aspect key `den.aspects.entropy`, includes `entropy-storage` + `entropy-device-capability-routing`, role workstation assert kept) and `storage/aarch64-darwin.nix` → `entropy.nix` (aspect key `entropy-storage`, keeps `includes = [ den.aspects.apple-silicon-hardware ]`).
- `device-capability-routing.nix:89`: `den.aspects.aarch64-darwin-device-capability-routing` → `den.aspects.entropy-device-capability-routing` (still `darwinRouting`).
- `hosts.nix`: added `darwinArm = machineFor "entropy"`, assert `darwinArm.target == "darwinConfigurations.entropy"`, and `den.hosts.aarch64-darwin.entropy` (inner host key `entropy` — the framework derives `darwinConfigurations.entropy` from it; the OUTER group key `aarch64-darwin` stays as the system). Follows the todo-10 gotcha (flake output name = inner host key).
- Also updated the stale comment in `modules/aspects/hosts/darwin-workstation.nix:3` ("selects aarch64-darwin by name" → "selects entropy by name") — it referenced the old HOST name, not the system.
- **Verification**: `nix eval --impure --json --expr 'builtins.attrNames (builtins.getFlake "path:/home/mei/nixos").darwinConfigurations'` → `["entropy"]`; `nixosConfigurations` unchanged `["aarch64-linux","antagony","nixos-x86-qualifier","remembrance"]`; `grep -Fq '"entropy"' model.nix` passes.
- **Pre-existing test debt (NOT from this todo)**: `tests/dendritic-config-eval.nix` already FAILED before todo 13 — line 124 asserts the pre-antagony 3-machine list. Its ready-expectation `darwinConfigurations == [ ]` (line 125) will flip to `[ "entropy" ]` when the config-eval test is rewritten to the post-remap topology (plan Todo 7/Momus). The old `aarch64-darwin` HOST name is gone from `modules/{aspects,entities}`; remaining `aarch64-darwin` references are all legitimate system-name uses (assertion messages, `systems.nix`-style routing, flake apps key).

## Todo 14 — drop `nixos-x86-qualifier` + `aarch64-linux` (eval hosts) (2026-08-23)

- Removed both eval hosts from `model.nix`, `validators.nix` (routing branches + the `isEvaluation` aarch64-linux closure in `validCrossFields` + the system enum at line 551), `hosts.nix` (`qualifier`/`linuxArm` bindings + `den.hosts` blocks), `outputs.nix` (configurationEvaluationPaths → `["nixosConfigurations.remembrance","darwinConfigurations.entropy"]`), and `defaults.nix` system enum.
- `git rm`'d the 6 aspect files (`named-hosts/{nixos-x86-qualifier,aarch64-linux}.nix`, `storage/{nixos-x86-qualifier,aarch64-linux}.nix`, `hardware/{pending-x86-qualifier,evaluation-aarch64}.nix`) and the whole `apps/aarch64-linux/` dir.
- `systems.nix` PINNED to `["x86_64-linux" "aarch64-darwin"]` (remove aarch64-linux, ADD aarch64-darwin) — the flake now emits `apps.aarch64-darwin` (mkDarwinApps: build/build-switch/clean/search-pkgs/update).
- `apps.nix`: removed the 4 `if system == "aarch64-linux"` branches (search-pkgs/home-switch/home-news/update), the `standalone-linux-aarch64` help lines, and the now-dead `mkScriptBackedApp` helper. `defaultTarget` collapsed to a literal `"standalone-linux"` (home-switch/home-news only build for x86_64-linux now).
- Python routing: `contracts.py` `_ROUTES` = {remembrance, aarch64-darwin}, vendor sets `{"remembrance","nixos-x86-qualifier"}` → `host == "remembrance"`, removed the aarch64-linux evaluation closure branch; `collector.py` `ROUTES` = {remembrance}. antagony deliberately NOT added (still no route → fixtures for it fail "routing" — consistent with todo-11 decision).
- Task15 F02 (`separate-host-closures`) could no longer use a second x86 host (only remembrance routed) → rewrote to Intel-vs-AMD vendor variants of the SAME host (remembrance) proving distinct digests. Task15 N20 model-parity aarch64-linux invalids → replaced with a false `aarch64-darwin` route declaration (fails darwin closure: `/home/mei` vs `/Users/mei`).
- **Gotcha (test debt that looks like my bug)**: `readiness task17 F03` fails in the runner unless `NIXOS_REPO_ROOT` is exported (run-case.sh's Python heredoc reads the ENV VAR, which the runner never sets). Not caused by this todo; pre-existing harness gap. With `NIXOS_REPO_ROOT=/home/mei/nixos` all task17 cases pass.
- **Gotcha (apps test)**: `dendritic-apps.sh` asserted `test ! -e apps/aarch64-darwin` — now that aarch64-darwin IS an eval system, the dir legitimately exists; flipped the assertion to positive (`test -d` + executable build/build-switch/clean) and added `apps/aarch64-darwin/build` to the boot-mutating-path grep.
- Verification: `grep -R -n --exclude-dir=.git -E 'nixos-x86-qualifier|aarch64-linux' .` → 0 product matches (only `.omo/` plan/evidence history retains the names, which the plan's own acceptance criterion excludes). nixosConfigurations `["antagony","remembrance"]`, darwinConfigurations `["entropy"]`, homeConfigurations `["standalone-linux"]`, machineAuthority.machines `[antagony entropy remembrance]`. config-eval/architecture/boundaries/apps/shells/package-policy + readiness task15 (both modes) + task17 (both modes) all PASS.

## Todo 15 — rewrite README.md for merged repo (2026-08-23)

- Replaced the entire old Install section (bin/nix-config-install / nix-config-enroll / install-direct / install-remote / nixos-laptop) with the ISO-driven flow: `nix build .#iso.<host>` (shorthand for `.#nixosConfigurations.<host>.config.system.build.isoImage`) → boot on target (Proxmox VM) → `nix run github:nix-community/nixos-anywhere -- --flake '.#<host>' --target-host <ip>` (auto-detects installer, skips kexec). Kept the four-enrollment gate (boot.state=uefi, storage.profile=single-gpt-btrfs, publicTrust.state=enrolled, secretTrust.state=enrolled) and pointed at `docs/service-notes/nixos-anywhere-iso-install.md` (todo 20).
- Hosts section now lists all 4 real hosts: remembrance (NixOS, this PC), antagony (NixOS, ThinkPad P52), entropy (macOS, Mac mini), standalone-linux (Home-Manager, massive, CachyOS). massive is NOT a NixOS machine — only the standalone-linux Home-Manager host.
- Layout `roles/` line trimmed to `workstation` (qualifier/evaluation dropped in todo 14). Duplication note rewritten: this is now the SINGLE merged repo, so the "duplicated bit-for-bit into both repos" language is gone — darwin + standalone-linux trees live here and share modules from this one repo.
- Acceptance: `grep -F 'bin/nix-config-install' README.md` → no match; `grep -Fq '.#iso.<host>' README.md` → match; no deleted-script refs (nix-config-install/enroll, install-direct/remote, nixos-anywhere-bootstrap-password); no dropped-host refs (nixos-laptop, nixos-x86-qualifier, aarch64-linux). Only README.md modified.

## Todo 16 — rewrite/delete dependent tests for deleted scripts (2026-08-23)

- Deleted `tests/bootstrap-password-install-helper.sh` (tests the deleted `bin/nixos-anywhere-bootstrap-password.fish`) and `tests/bootstrap-password-mutations.sh` (mutates the deleted `.sh`). The plan said DELETE all four bootstrap-password tests, but the task instructions refined this: `lifecycle.sh` and `secret-scan.sh` are KEPT because they test the KEPT `modules/nixos/bootstrap-password.nix` module / the repo-wide secret scanner, not the deleted bin scripts.
- `tests/readiness/task7/run_case.py`: removed the N19-direct-unenrolled + N20-remote-disabled branches (they subprocess `bin/install-direct`/`bin/install-remote`). ALSO removed those two case entries from `tests/readiness/cases/task-7.json` — otherwise the runner would run them and get exit 2 (unknown selector) which falsely "matches" exit-nonzero. Manifest must stay canonical (`json.dumps(sort_keys=True, separators=(",",":")) + "\n"`).
- `tests/readiness/task7/test-static.sh`: removed the `test -x bin/install-direct` / `test -x bin/install-remote` / `if bin/install-remote ...` block. Kept the `physical-install-requires-attended-run` assertion on `scripts/readiness/task7/installer.py` (contract string, not a script reference).
- `scripts/readiness/task7/installer.py`: the bin/install-direct + bin/install-remote are shims to `bin/nix-config-install --mode={direct,remote}` (NOT to installer.py). installer.py is the standalone Task-7 contract module. Removed the `main`/`remote_main`/`__main__` CLI entry points (the install-direct/install-remote CLIs = references to the deleted scripts); kept `run_direct`/`run_remote` + helpers. `physical-install-requires-attended-run` string intact (test-static asserts it).
- `tests/bootstrap-password-lifecycle.sh`: KEPT. Only stale reference was the flake host name `nixosConfigurations.x86_64-linux` (dropped in todo 14) → `nixosConfigurations.remembrance`. Verified the module's activation scripts resolve for remembrance and the full lifecycle test PASSES.
- `tests/bootstrap-password-secret-scan.sh`: KEPT (no deleted-script references; `bin/helper.fish` is a test fixture, not a reference). NOTE pre-existing failure NOT from this todo: the manifest-complete check compares the Python capture (sorted paths) against the bash `git ls-files` order (untracked first). The untracked `modules/aspects/{named-hosts,storage}/antagony.nix` (todo 11, never committed) break the order → `cmp` differ. Will self-heal when the antagony files are committed (todo 19) or the bash side sorts.
- `tests/readiness/task15/*` + `task22`: KEPT untouched (reference the KEPT hardware-collector/intake + home_preflight.py). task17 KEPT.
- Verification: readiness runner PASS for task7 (6 fixture + 21 negative), task15 (3 fixture + 21 negative), task17 (3 fixture with `NIXOS_REPO_ROOT=/home/mei/nixos` — known pre-existing harness gap + 3 negative). `python3 -m pytest tests/readiness/task15 tests/readiness/task17 scripts/hardware` collects 0 items (exit 5) — the repo has NO pytest-style `def test_*` functions; all readiness tests are script-style (`/// script` blocks run via run-case.sh). The acceptance criterion means "pytest runs without collection errors" (the `ModuleNotFoundError: No module named 'tests'` is avoided by running from the repo root / PYTHONPATH).
- Acceptance grep: `grep -R -n --exclude-dir=.git --exclude-dir=.omo -E 'bin/nix-config-install|bin/nix-config-enroll|bin/install-direct|bin/install-remote|bin/nixos-anywhere-bootstrap-password' tests/ scripts/` → 0 matches. Also 0 matches for the bare names (no `bin/` prefix) in tests/.
- Pre-existing (NOT from this task): `test-static.sh` `rg -Fq 'ERASE' docs/service-notes/nixos-anywhere-disko-install.md` fails — the merged docs never had `ERASE` (todo 20 docs rewrite will fix). `secret-scan.sh` manifest-order failure (untracked antagony files, above).

## Todo 17 — ISO-builder flake target `.#iso.<host>` (2026-08-23)

- Created `modules/flake/iso-images.nix` (auto-loaded by import-tree alongside apps.nix/packages.nix). Exposes `flake.iso.remembrance` + `flake.iso.antagony` → `nix build .#iso.<host>` works. Exactly ONE variant (installer) per NixOS host.
- **Gotcha 1 (nixpkgs 26.11 rename)**: `config.system.build.isoImage` no longer exists on a plain host eval; the ISO builder lives at `config.system.build.images.iso` (nixos/modules/image/images.nix, iso-image.nix moved under image modules + `system.build.image = isoImage` alias). Use `.config.system.build.images.iso`. A bare `extendModules` eval has NO `system.build.isoImage` — it only exists on the iso-extended eval.
- **Gotcha 2 (initrd)**: both hosts set `nixos.boot.initrd.enable = false` via the `pending-x86-workstation-hardware` aspect (boot.state == "disabled" / pending enrollment). iso-image.nix unconditionally references `config.system.build.initialRamdisk` (in `isoImage.contents` when showConfiguration) → eval error "attribute 'initialRamdisk' missing". Preliminary `extendModules { modules = [ { boot.initrd.enable = true; } ] }` gives "conflicting definition values" — needs `lib.mkForce true` (aspect def has default priority).
- Final shape: `isoFor host = (config.flake.nixosConfigurations.${host}.extendModules { modules = [ { boot.initrd.enable = lib.mkForce true; } ]; }).config.system.build.images.iso`. `lib.genAttrs [ "remembrance" "antagony" ] isoFor` under `flake.iso`. Accessing `config.flake.nixosConfigurations` from a top-level flake module works (same value as the `nixosConfigurations` output).
- Verification: `nix eval` of `.iso.remembrance`/`.iso.antagony` → `/nix/store/...-nixos-26.11.20260818.0ae2bc1-x86_64-linux.iso` derivation paths; flake still evaluates (`nixosConfigurations == ["antagony","remembrance"]`, `flake ? iso == true`). ISO NOT built (deferred to QA wave). Evidence: `.omo/evidence/consolidate-nixos-repo/task-17-iso-builder-flake-target.json`.
- manifest.schema.json already requires `isoArtifactSizeBytes` (integer ≥2048, multipleOf 2048) + `isoSha256` (`^[0-9a-f]{64}$`); contracts.py lines 776-777 (required-fields), 821 (assert-set), 840 (int > 0 && % 2048) — both already wired, no changes needed.
- README's existing `nix build .#iso.<host>` install docs (todo 15) now match reality.

## Todo 18 — Delete the 6 manual-ssh install scripts (2026-08-23)

- Deleted via `git rm`: `bin/nix-config-install`, `bin/nix-config-enroll`, `bin/install-direct`, `bin/install-remote`, `bin/nixos-anywhere-bootstrap-password.sh`, `bin/nixos-anywhere-bootstrap-password.fish`. All 6 staged as `D`.
- KEPT (on disk, still present after rm): `bin/nix-config-hardware-collector`, `bin/nix-config-hardware-intake`, `bin/setup-noctalia-cachix.sh`.
- No `bin/__pycache__/` existed for the deleted scripts (no Python in bin/).
- Flake/apps clean: `grep --include='*.nix' -E 'nix-config-install|nix-config-enroll|install-direct|install-remote|nixos-anywhere-bootstrap-password' modules/ flake.nix apps/` → 0 matches.
- RESIDUAL (NOT fixed by this todo, per instruction to not modify other files): `docs/service-notes/nixos-anywhere-disko-install.md` still documents the 6 deleted scripts (lines 6, 55, 66, 76, 79, 88, 92, 99-100). This is the pre-existing todo-20 docs-rewrite target already noted in the todo-16 learnings. The acceptance grep → 0 (except evidence/snapshot) fails ONLY on this one doc.
- Evidence: `.omo/evidence/consolidate-nixos-repo/task-18-consolidate-nixos-repo.json`.

## Todo 20 — ISO-first nixos-anywhere install + enrollment-writer verification (2026-08-23)

- Created `docs/service-notes/nixos-anywhere-iso-install.md`: build `nix build .#iso.<host>` (remembrance, antagony; shorthand for `.#nixosConfigurations.<host>.config.system.build.isoImage`), boot on Proxmox VM (OVMF/UEFI, ≥4GB RAM, whole-device disk), then `nix run github:nix-community/nixos-anywhere -- --flake '.#<host>' --target-host root@<ip>`. The ISO sets `VARIANT_ID=installer` in `/etc/os-release` (NixOS 23.05+), so nixos-anywhere auto-detects the installer and SKIPS kexec (verified against upstream docs/howtos/no-os.md). Kept the four-enrollment gate + day-2 deploy-rs pointer.
- Scrubbed `docs/service-notes/nixos-anywhere-disko-install.md`: rewrote the deleted-script flow (bin/nix-config-install / nix-config-enroll / install-direct / install-remote / bootstrap-password) into the ISO-first flow. KEPT the `ERASE <hostId> <diskById> <diskIdentitySha256> <deviceBindingSha256>` contract — `tests/readiness/task7/test-static.sh` line 9 asserts `rg -Fq 'ERASE'` on this doc (todo-16 learnings flagged this as a pre-existing failure the todo-20 rewrite would fix; it now passes).
- **Enrollment-writer verification (ORACLE P1-1)**: the real intake CLI surface is `collect|create|validate` (NO `--fixture` flag — that lives on the collector). Verified end to end with the real CLI:
  - `collect source.json` → enrolled declaration (exit 0)
  - `create base.json candidate.json operator 2026-08-23T12:00:00Z` → canonical RFC-6902 intake document (exit 0) — this is the enrollment WRITER
  - `validate base.json intake.json` → validated candidate (exit 0) — this is the KEPT intake VALIDATOR
  - Validated record has all four enrollments: boot.state=uefi, storage.profile=single-gpt-btrfs, publicTrust.state=enrolled, secretTrust.state=enrolled.
- **Gotcha (task spec vs reality)**: the task's literal fixture spec used hostId `antagony`, but antagony is deliberately NOT in the routing key (`contracts.py` `_ROUTES` = {remembrance, aarch64-darwin}; todo-14 decision). Empirically confirmed: `validate_declaration` on an antagony record raises `routing`. The acceptance test therefore uses `remembrance` (the canonical routed x86 host, same as the task15 fixtures). The intake document shape still matches the task spec exactly (schemaVersion 1, hostId, inputDeclarationDigest, patch, patchSha256, outputDeclarationDigest, reviewedBy, appliedAt).
- **Gotcha (canonical JSON)**: `canonical_json.encode()` already appends `\n`; writing `encode(v) + b"\n"` produces a double-LF file that `require_canonical` rejects ("JSON bytes are not canonical"). Write `encode(v)` verbatim.
- Acceptance: `grep -R -n --exclude-dir=.git --exclude-dir=.omo -E 'bin/nix-config-install|bin/nix-config-enroll|bin/install-direct|bin/install-remote|bin/nixos-anywhere-bootstrap-password' .` → 0 matches. `bash tests/readiness/task7/test-static.sh` → TASK 7 STATIC PASS. Readiness task7 fixture + negative both PASS. Evidence: `.omo/evidence/consolidate-nixos-repo/task-20-consolidate-nixos-repo.json` + `task-20-fixtures/`.

## Todo 19 — wire deploy-rs (`deploy.nodes.<host>` + `deployChecks`) (2026-08-23)

- Added `deploy-rs.url = "github:serokell/deploy-rs";` to flake.nix (after `import-tree`). `nix flake lock` pinned `github:serokell/deploy-rs/1690127` (2026-08-10) + its flake-compat/nixpkgs/utils inputs (deploy-rs keeps its OWN nixpkgs pin).
- Created `modules/flake/deploy-rs.nix` (auto-loaded by import-tree). Module args `{ inputs, lib, ... }` with `inherit (inputs) self deploy-rs;` — the repo pattern does NOT pass `self` directly; use `inputs.self` (apps.nix/checks.nix convention).
- Emits `flake.deploy.nodes.<host>` (freeform `flake` submodule attrs) + `flake.checks`. `flake.checks` is the flake-parts transposed per-system option (`mkTransposedPerSystemModule`, type `lazyAttrsOf (lazyAttrsOf package)`) — the deploy-rs `builtins.mapAttrs (system: deployLib: deployLib.deployChecks self.deploy) deploy-rs.lib` map keyed by system merges cleanly with the existing perSystem checks.
- Deploy API (VERIFIED against deploy-rs master flake.nix, NOT the task's sketch): activators are `activate.nixos <nixosConfig>`, `activate.darwin <darwinConfig>`, `activate.home-manager <homeConfig>` (there is NO `activate.home`). Task's `activate.home` for massive → use `activate.home-manager`. massive is home-only: `profiles.home = { user = "mei"; path = deploy-rs.lib.x86_64-linux.activate.home-manager self.homeConfigurations.standalone-linux; }`.
- `deployChecks self.deploy` is safe in flake-parts: `self.deploy` = top-level deploy output (nodes only, `checks` is a sibling output), so no circular reference. `deploy-schema` runs `builtins.toJSON deploy` — in Nix 2.34 `toJSON` on a derivation yields its store-path STRING (verified), so schema check evaluates fine.
- KEY acceptance: `nix eval --impure --json --expr 'builtins.attrNames (builtins.getFlake "path:/home/mei/nixos").deploy.nodes'` → `["antagony","entropy","massive","remembrance"]`. All 4 deploy-node path `.drvPath`/`.outPath` evaluate; all 4 systems' `deploy-schema`/`deploy-activate` check drvs evaluate (including aarch64-linux/x86_64-darwin from deploy-rs.lib).
- **PRE-EXISTING failure (NOT this todo)**: `nix flake check --all-systems --no-build` fails ONLY on `nixosConfigurations.antagony` with `The option 'home-manager.users.mei.home.stateVersion' was accessed but has no value defined`, triggered via `system.build.toplevel` → `assertions` → nixpkgs `nixos/common.nix` → `modules/programs/aerospace.nix` (nixpkgs HMi module that reads `config.home.homeDirectory`). PROVEN pre-existing: removing `modules/flake/deploy-rs.nix` (keeping the input) reproduces the identical failure. antagony `.toplevel.drvPath`/`.outPath` and `config.home-manager.users.mei.home.stateVersion` all eval correctly in isolation — only flake-check's full toplevel/assertions walk trips it. Out of scope (MUST NOT modify host aspects). Every other check passes: remembrance/entropy/standalone-linux, all 6 dendritic checks × 2 systems, all deploy checks.

## Todo 21 — flake-check gate: untracked files break `nix flake check` (2026-08-23)

- **SYMPTOM**: `nix flake check --system x86_64-linux --no-build` fails ONLY on `nixosConfigurations.antagony`: `The option 'home-manager.users.mei.home.stateVersion' was accessed but has no value defined`.
- **UNDERLYING FACT**: `nix flake check` on a GIT flake evaluates the git-tracked source (untracked files are EXCLUDED from the evaluation snapshot). This is different from `path:` flakes and `git+file://` with explicit revs (which include untracked files as-is). Empirically: wrapper flakes that re-export the repo via `path:` input PASS; `git+file:///home/mei/nixos` input FAILS identically; a non-git copy fails/passes same as path.
- **REAL ROOT CAUSE (was mis-diagnosed as home-manager/aerospace)**: antagony-specific files were still UNTRACKED: `modules/aspects/named-hosts/antagony.nix`, `modules/aspects/storage/antagony.nix`, plus the todo-19/20 flake modules `modules/flake/deploy-rs.nix` and `modules/flake/iso-images.nix`. In git-mode eval, `den.aspects.antagony` doesn't exist → the antagony NixOS config is assembled without `den.aspects.linux-desktop` → `home-manager.useGlobalPkgs`/`useUserPackages`/`stateVersion` from `modules/nixos/home-manager.nix` never get wired → home-manager's aerospace HM module runs through `misc/nixpkgs.nix` (`useNixpkgsModule=true`) → resolving its `pkgsPath` module argument forces `config.home.stateVersion` during `_module.args` resolution → "no value defined".
- **THE MISLEADER**: remembering never trips it because its aspects live in TRACKED files, so git-mode builds it fully and correctly. Looking at the aerospace upgrade/stateVersion cycle was a red herring.
- **FIX (minimal, 1 step)**: `git add` the four untracked eval-relevant files so they're part of the tracked source: `git add modules/aspects/named-hosts/antagony.nix modules/aspects/storage/antagony.nix modules/flake/deploy-rs.nix modules/flake/iso-images.nix`. No Nix files changed. (The repo's whole consolidated tree is itself uncommitted-on-main; that's the todo-21 commit gate, unchanged.)
- **VERIFICATION**: `nix flake check --system x86_64-linux --no-build` → exit 0 (remembrance AND antagony both pass, `(build skipped)`). Toplevel `.drvPath`s UNCHANGED vs pre-fix (byte-identical store paths), `config.home-manager.users.mei.home.stateVersion` still `"21.05"`, `programs.aerospace.enable` still `false` on both, `.deploy.nodes` = [antagony entropy massive remembrance], `.iso.<host>` outPaths resolve.
- **Gotcha**: Not all untracked files matter — `.omo/evidence/*`, `docs/service-notes/nixos-anywhere-iso-install.md` etc are inert for flake eval. Only files reachable from `outputs = mkFlake ... (import-tree ./modules/flake)` + aspect tree matter for `flake check`.
- **Gotcha**: native flake-eval (git) excludes untracked; path/impure evals (`builtins.getFlake "path:..."`) include them. So a config can pass `nix eval --impure` while failing `nix flake check` — always sanity check with the real harness when only `nix flake check` fails.
- **task17 F03 readiness fix**: `tests/readiness/task17/run-case.sh` F03 reads `modules/standalone-linux/packages.nix` via `os.environ.get("NIXOS_REPO_ROOT", "")`, which was empty when the runner didn't set it → `FileNotFoundError: '/modules/standalone-linux/packages.nix'`. **FIX (1 line)**: in `tests/readiness/runner.py` `run_case()`, add `"NIXOS_REPO_ROOT": str(root)` to the subprocess env dict (`root = Path(__file__).resolve().parents[2]` = repo root). No test assertions or product code touched.
- **VERIFICATION**: `bash tests/readiness/run-task.sh 17 fixture` → PASS (F01/F02/F03 all matched, rc=0), `17 negative` → PASS, `15 fixture`/`15 negative` → PASS, `7 fixture` → PASS — all without manually setting NIXOS_REPO_ROOT.

## F4 scope-fidelity: stale host references (host rename darwin→entropy, add antagony)
- **contracts.py `_ROUTES`** is the route authority for `validate_declaration`: maps hostId → (target, system, role). Updated to `remembrance`/`antagony` (nixosConfigurations.*) + `entropy` (darwinConfigurations.entropy). The `aarch64-darwin` STRING survives ONLY as the **system** triple inside the entropy tuple + `top["system"] == "aarch64-darwin"` checks — MUST NOT remove (it's the OS platform, not the host).
- **contracts.py per-host closure branches** (formerly `host == "remembrance"` / `host == "aarch64-darwin"`): x86 branches now apply to `{"remembrance", "antagony"}` (pending-vendor closure + full-enrollment closure); darwin branch is `host == "entropy"` → error label `"entropy closure"`.
- **collector.py `ROUTES`** is deliberately x86-only (collector rejects Apple/unknown vendors) → only remembrance+antagony belong there, NOT entropy.
- **Gotcha — the F4 grep `-E 'aarch64-darwin|...'` can NEVER reach 0 by design**: MUST-NOT rule keeps the `aarch64-darwin` OS system name in data fields (`"system": "aarch64-darwin"` in task15/task17 fixtures, contracts system comparisons, dendritic-apps.sh). The real acceptance grep is on host-level constructs: `darwinConfigurations\.aarch64-darwin`, `nixosConfigurations\.x86_64-linux`, `"hostId": "aarch64-darwin"`, `host == "aarch64-darwin"` → all 0.
- **task17/model.py `production_machines()`** (fixture) held a stale `"hostId": "aarch64-darwin"` even though F4 only flagged task15 — same class of stale host ref (validate() ignores hostId, so renaming to `entropy` changes no assertion). Update both when renaming hosts.
- **VERIFICATION**: grep 0 for all host-route constructs; `run-task.sh 15 fixture/negative, 17 fixture/negative, 7 fixture` ALL PASS; `nix flake check --all-systems --no-build` → exit 0; `py_compile` clean on all 7 edited .py files.
